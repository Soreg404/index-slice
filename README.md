# Non-borrowing slice | index slice

## advantages:
- the buffer of a slice can stay mutable

## disadvantages:
- the buffer of a slice can stay mutable

## Goal
View into a buffer can be cached in a struct during parsing

