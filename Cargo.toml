[package]
name = "index_slice"
version = "0.1.2"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Simple shortcut struct for storing views / indices into buffer without borrow"
repository = "https://github.com/Soreg404/index-slice"

